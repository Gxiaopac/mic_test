# Netlify 部署检查清单

## ✅ 已完成的配置

- [x] **runtime.txt** - 指定 Python 3.11.7（有预构建的 numpy 轮子）
- [x] **netlify.toml** - Netlify 构建配置
- [x] **requirements.txt** - 更新为兼容版本范围
- [x] **netlify/functions/api.py** - Netlify Functions 包装器
- [x] **static/app.js** - 更新所有 API 调用支持 Netlify Functions
- [x] **.gitignore** - 排除不需要的文件

## 📋 部署前检查

### 1. 文件完整性检查

确保以下文件存在：
```
MicTester_v2.10_Web/
├── app.py
├── config.json
├── requirements.txt
├── runtime.txt          ✅
├── netlify.toml         ✅
├── netlify/
│   └── functions/
│       └── api.py       ✅
├── templates/
│   └── index.html
└── static/
    ├── app.js           ✅ (已更新 API 路径)
    └── style.css
```

### 2. requirements.txt 检查

确认包含：
- `numpy>=1.24.0,<1.26.0` ✅ (兼容 Python 3.11)
- `serverless-wsgi==0.8.2` ✅ (Netlify Functions 必需)

### 3. 代码检查

- [x] `app.js` 中所有 `/api/` 调用已更新为 `${API_BASE}/api/`
- [x] `API_BASE` 变量已定义（本地为空，生产环境为 `/.netlify/functions/api`）

## 🚀 部署步骤

### 步骤 1: 提交代码到 GitHub

```bash
cd E:\testtool_1\MicTester_v2.10_Web
git add .
git commit -m "Add Netlify deployment configuration with Python 3.11"
git push
```

### 步骤 2: 在 Netlify 创建站点

1. 登录 [Netlify](https://app.netlify.com)
2. 点击 "Add new site" → "Import an existing project"
3. 选择 GitHub 并授权
4. 选择仓库：`MicTester_v2.10_Web`

### 步骤 3: 配置构建设置

在 Netlify 站点设置中：

**Build settings:**
- **Build command**: `pip install -r requirements.txt`
- **Publish directory**: `.` (或留空)
- **Functions directory**: `netlify/functions`

**Environment variables (可选):**
- `PYTHON_VERSION`: `3.11`

### 步骤 4: 部署

点击 "Deploy site"

## 🔍 部署后验证

### 1. 检查构建日志

在 Netlify 的 Deploys 页面查看构建日志，确认：
- ✅ Python 版本是 3.11.x
- ✅ numpy 成功安装（使用预构建轮子，不是从源代码编译）
- ✅ 所有依赖安装成功
- ✅ Netlify Functions 构建成功

### 2. 测试功能

访问部署的网站，测试：
- [ ] 页面正常加载
- [ ] API 调用正常（检查浏览器控制台）
- [ ] 麦克风权限请求正常
- [ ] 录音功能正常
- [ ] 音频分析功能正常

### 3. 常见问题

**问题：numpy 安装失败**
- 检查 `runtime.txt` 是否指定 Python 3.11
- 检查构建日志中的 Python 版本
- 确认 `requirements.txt` 中的 numpy 版本范围

**问题：API 404 错误**
- 检查 `netlify.toml` 中的 redirects 配置
- 检查 `netlify/functions/api.py` 是否存在
- 查看 Netlify Functions 日志

**问题：静态文件无法加载**
- 确认 `static/` 和 `templates/` 目录在仓库中
- 检查 `netlify.toml` 的 publish 设置

## 📝 后续维护

### 更新代码

```bash
git add .
git commit -m "Update: 描述更改"
git push
```

Netlify 会自动重新部署。

### 查看日志

- Netlify Dashboard → Site → Functions → 查看函数日志
- Netlify Dashboard → Site → Deploys → 查看构建日志

## 🎯 关键配置说明

### runtime.txt
```
python-3.11.7
```
**作用**: 告诉 Netlify 使用 Python 3.11.7，这个版本有预构建的 numpy 轮子。

### netlify.toml
```toml
[build.environment]
  PYTHON_VERSION = "3.11"
```
**作用**: 进一步确保使用 Python 3.11。

### requirements.txt
```
numpy>=1.24.0,<1.26.0
```
**作用**: 使用版本范围，确保兼容 Python 3.11，且有预构建轮子。

## ⚠️ 重要提示

1. **首次部署可能需要 5-10 分钟**（安装依赖）
2. **确保所有文件已提交到 GitHub**
3. **检查构建日志确认 Python 版本**
4. **测试所有功能确保正常工作**

